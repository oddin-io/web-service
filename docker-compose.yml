version: "2"
services:
  postgres:
    image: postgres:9.5.3
    environment:
      POSTGRES_USER: ws-oddin
      POSTGRES_PASSWORD: 123
    volumes:
      - web-service-database:/var/lib/postgresql/data
    expose:
      - 5432

  web-service:
    build: .
    links:
      - postgres
    ports:
      - '80:3000'
    volumes:
      - .:/app
    env_file:
      - .env
volumes:
  web-service-database:
    driver: local
